//state chung cho dự án
const initialState = {
  userLogged: null,
};

// reducer cho dự án
const reducers = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default reducers;
